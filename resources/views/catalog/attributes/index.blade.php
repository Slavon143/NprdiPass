<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-medium text-indigo-600">{{ __('Catalog') }}</p><h1 class="mt-1 text-2xl font-bold text-slate-900">{{ __('Attributes') }}</h1><p class="mt-1 text-sm text-slate-500">{{ __('Reusable Product and Variant characteristics for :company.', ['company' => $company->name]) }}</p></div>@if($canManage)<a href="{{ route('catalog.attributes.create') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-center text-sm font-semibold text-white hover:bg-indigo-500">{{ __('Create attribute') }}</a>@endif</div></x-slot>
    <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">{{ __('Attribute') }}</th><th class="px-5 py-3">{{ __('Type / Scope') }}</th><th class="px-5 py-3">{{ __('Flags') }}</th><th class="px-5 py-3">{{ __('Options / Usage') }}</th><th class="px-5 py-3">{{ __('Status') }}</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">
        @forelse($definitions as $definition)<tr><td class="px-5 py-4"><p class="font-semibold text-slate-900">{{ $definition->name }}</p><p class="font-mono text-xs text-slate-500">{{ $definition->code }}@if($definition->unit) · {{ $definition->unit }}@endif</p></td><td class="px-5 py-4"><span class="capitalize">{{ $definition->type->value }}</span> · <span class="capitalize">{{ $definition->scope->value }}</span></td><td class="px-5 py-4 text-xs text-slate-600">{{ $definition->required ? __('Required') : __('Optional') }} · {{ $definition->filterable ? __('Filterable') : __('Not filterable') }} · {{ $definition->searchable ? __('Searchable') : __('Not searchable') }}</td><td class="px-5 py-4">{{ $definition->options_count }} / {{ $definition->product_values_count + $definition->variant_values_count }}</td><td class="px-5 py-4"><x-badge :tone="$definition->status->value === 'active' ? 'green' : 'gray'">{{ ucfirst($definition->status->value) }}</x-badge></td><td class="px-5 py-4 text-right"><a href="{{ route('catalog.attributes.show', $definition->uuid) }}" class="font-semibold text-indigo-700 hover:text-indigo-900">{{ __('View') }}</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">{{ __('No attributes yet.') }}</td></tr>@endforelse
        </tbody></table></div></div><div class="mt-5">{{ $definitions->links() }}</div></div>
</x-app-layout>
